# GENERATED by scripts/apply-theme.sh from themes/{{meta.name}}.toml -
# edit themes/<name>.toml or themes/templates/treelix.toml.tmpl, NOT this file.
#
# Format:
#   [palette]   name = "#RRGGBB"
#   [styles]    element = "fg" | "fg / bg" | "fg / bg / mod,mod"
#               where fg/bg is a palette name, a #hex value, or "none".
#               modifiers: bold, italic, underline, dim, reversed.

[palette]
bg           = "{{ui.bg}}"
bgSurface    = "{{ui.bg_surface}}"
bgHighlight  = "{{ui.bg_highlight}}"
bgHighlight2 = "{{ui.bg_highlight2}}"
fg           = "{{ui.fg}}"
fgBright     = "{{ui.fg_bright}}"
fgMuted      = "{{ui.fg_muted}}"
accent       = "{{ui.accent}}"
accentAlt    = "{{ui.accent_alt}}"
folder       = "{{ui.folder}}"
red    = "{{colors.red}}"
orange = "{{colors.orange}}"
yellow = "{{colors.yellow}}"
green  = "{{colors.green}}"
purple = "{{colors.purple}}"

[styles]
text          = "fg"
# "none" keeps the terminal-default background so ghostty transparency
# shows through the sidebar (mirrors helix `ui.background = {}`).
background    = "none"
selection     = "none / bgHighlight2"
directory     = "folder / none / bold"
root          = "fgBright / none / bold"
indent_marker = "fgMuted"
arrow         = "fgMuted"
icon          = "fg"
folder_icon   = "folder"
symlink       = "accentAlt"
# Plain text color: the Nerd Font icon already marks scripts, and green here
# collided with git_staged, making every `chmod +x` script read as staged
# forever ("my .sh files stay green").
executable    = "fg"
git_staged    = "green"
git_dirty     = "yellow"
git_deleted   = "red"
git_renamed   = "yellow"
git_conflict  = "red"
git_untracked = "orange"
git_ignored   = "fgMuted"
# LSP diagnostics pushed by the patched helix (sidebar_follow.rs): file names
# and their folders take the worst severity's color. These win over the git
# colors above; the git glyph after the name still shows the git state.
diagnostic_error   = "red"
diagnostic_warning = "yellow"
cut           = "red / none / italic"
copy          = "yellow"
# Bold only, normal text color (nvim-tree's SpecialFile convention): a
# status COLOR here is a trap — yellow collided with git_dirty, making
# curated "special" files (package.json, README.md, ...) read as modified.
special       = "fg / none / bold"
opened        = "fgBright / none / bold"
bookmark      = "purple"
selected      = "none / bgHighlight"
help          = "fg"
help_title    = "accent / none / bold"
prompt        = "orange / none / bold"
filter_prefix = "purple / none / bold"
