# Claude Code session state
.claude/

# macOS
.DS_Store

# Third-party plugin cloned by scripts/setup.sh — not tracked here, the
# script keeps it current via `git pull --ff-only` (handled by update.sh).
zsh-helix-mode/

# mise leaves timestamped backups when configs are first trusted.
*.toml-[0-9]*
